{% block topi_cart_rental_summry_label_script %}Object.values({{ products|map(p => p.getExtension(extensionName))|json_encode|raw }})

    <script>
        window.addEventListener("load", (event) => {
          topi.cartItems = Object.values({{ products|map(p => p.getExtension(extensionName))|json_encode|raw }})
        });
    </script>
{% endblock %}
